package com.netra.ui.theme

// Compose removed; keep placeholders to satisfy references if any
object ThemeColors {
    const val PrimaryHex: Long = 0xFF00BFFF
    const val SecondaryHex: Long = 0xFFFFD700
}